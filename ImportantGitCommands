git init
git add
git commit 
git status
git config
git branch newBranchNmae
git branch 
git checkout newBranch
Edit some files
In order to merge.
git checkout master
git merge  new branchname
delete new branch
git branch -d branchname
 git remote add origin sshlink
now local repository is connected with remote repository
mkdir createrepo
cd createrepo
git clone ssh link
now project is cloned from remote repository to local repository
iN git hub if we made any changes ,inorder to clone those changes we have to use
git pull origin main
all changes are added to local machine.
Now we need to add some content in local repository and we need to push
